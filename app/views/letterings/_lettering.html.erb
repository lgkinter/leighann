<% @letterings.each do |lettering| %>
  <div class="grid-item" id="lettering-<%= lettering.id %>">
    <div class="lettering_pic">
      <%= link_to(project) do %>
        <%= image_tag(lettering.image.url) %>
      <% end %>
    </div>
</div>
<% end %>
